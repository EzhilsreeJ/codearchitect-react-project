import React from 'react';function LogCenter() {  const logs = [    "INFO: Project initialized successfully.",    "WARNING: Missing .env file, using default configurations.",    "ERROR: Build failed at 2023-10-27 10:30:05.",    "INFO: Git commit 'Initial commit' successful.",    "DEBUG: Component re-rendered."  ];  return (    <div className="log-center">      <h2>Application Logs</h2>      <div className="log-list">        {logs.map((log, index) => (          <p key={index} className={`log-entry ${log.split(':')[0].toLowerCase()}`}>            {log}          </p>        ))}      </div>    </div>  );}export default LogCenter;