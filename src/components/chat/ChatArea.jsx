import React, { useState } from 'react';import MessageList from './MessageList.jsx';import ChatInput from './ChatInput.jsx';function ChatArea() {  const [messages, setMessages] = useState([    { id: 1, sender: 'AI', text: 'Hello! How can I help you today?' },    { id: 2, sender: 'User', text: 'Show me my recent project files.' }  ]);  const handleSendMessage = (text) => {    if (text.trim()) {      setMessages(prevMessages => [...prevMessages, { id: prevMessages.length + 1, sender: 'User', text }]);    }  };  return (    <div className="chat-area">      <h2>Chat with CodeArchitect AI</h2>      <MessageList messages={messages} />      <ChatInput onSendMessage={handleSendMessage} />    </div>  );}export default ChatArea;