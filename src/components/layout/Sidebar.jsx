import React from 'react';function Sidebar({ setActivePanel, activePanel }) {  return (    <aside className="sidebar">      <div className="sidebar-header">        <h3>CodeArchitect</h3>      </div>      <nav>        <ul>          <li>            <button className={activePanel === 'chat' ? 'active' : ''} onClick={() => setActivePanel('chat')}>Chat</button>          </li>          <li>            <button className={activePanel === 'code-viewer' ? 'active' : ''} onClick={() => setActivePanel('code-viewer')}>Code Viewer</button>          </li>          <li>            <button className={activePanel === 'git' ? 'active' : ''} onClick={() => setActivePanel('git')}>Git</button>          </li>          <li>            <button className={activePanel === 'deploy' ? 'active' : ''} onClick={() => setActivePanel('deploy')}>Deploy</button>          </li>          <li>            <button className={activePanel === 'logs' ? 'active' : ''} onClick={() => setActivePanel('logs')}>Logs</button>          </li>        </ul>      </nav>    </aside>  );}export default Sidebar;